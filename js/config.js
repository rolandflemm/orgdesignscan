// OrgDesignScan — API Configuration
// Replace the empty string below with your OpenAI API key to enable live diagnosis.
// Without a key the survey runs in demo mode with a sample result.

window.ODS_CONFIG = {
  apiKey: '',
};
